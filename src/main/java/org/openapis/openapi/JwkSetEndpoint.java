/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.RequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.Boolean;
import java.lang.Exception;
import java.lang.String;
import org.openapis.openapi.models.operations.ServiceJwksGetApiRequest;
import org.openapis.openapi.models.operations.ServiceJwksGetApiRequestBuilder;
import org.openapis.openapi.models.operations.ServiceJwksGetApiResponse;
import org.openapis.openapi.operations.ServiceJwksGetApiOperation;


public class JwkSetEndpoint {
    private final SDKConfiguration sdkConfiguration;

    JwkSetEndpoint(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
    }

    /**
     * Get JWK Set
     * 
     * <p>This API gathers JWK Set information for a service so that its client applications can verify
     * signatures by the service and encrypt their requests to the service.
     * 
     * <p>&lt;br&gt;
     * &lt;details&gt;
     * &lt;summary&gt;Description&lt;/summary&gt;
     * 
     * <p>This API is supposed to be called from within the implementation of the jwk set endpoint of the
     * service where the service that supports OpenID Connect must expose its JWK Set information so that
     * client applications can verify signatures by the service and encrypt their requests to the service.
     * The URI of the endpoint can be found as the value of `jwks_uri` in [OpenID Provider Metadata](https://openid.net/specs/openid-connect-discovery-1_0.html#ProviderMetadata)
     * if the service supports [OpenID Connect Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html).
     * 
     * <p>&lt;/details&gt;
     * 
     * @return The call builder
     */
    public ServiceJwksGetApiRequestBuilder get() {
        return new ServiceJwksGetApiRequestBuilder(sdkConfiguration);
    }

    /**
     * Get JWK Set
     * 
     * <p>This API gathers JWK Set information for a service so that its client applications can verify
     * signatures by the service and encrypt their requests to the service.
     * 
     * <p>&lt;br&gt;
     * &lt;details&gt;
     * &lt;summary&gt;Description&lt;/summary&gt;
     * 
     * <p>This API is supposed to be called from within the implementation of the jwk set endpoint of the
     * service where the service that supports OpenID Connect must expose its JWK Set information so that
     * client applications can verify signatures by the service and encrypt their requests to the service.
     * The URI of the endpoint can be found as the value of `jwks_uri` in [OpenID Provider Metadata](https://openid.net/specs/openid-connect-discovery-1_0.html#ProviderMetadata)
     * if the service supports [OpenID Connect Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html).
     * 
     * <p>&lt;/details&gt;
     * 
     * @param serviceId A service ID.
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public ServiceJwksGetApiResponse get(@Nonnull String serviceId) throws Exception {
        return get(null, null, serviceId,
            null);
    }

    /**
     * Get JWK Set
     * 
     * <p>This API gathers JWK Set information for a service so that its client applications can verify
     * signatures by the service and encrypt their requests to the service.
     * 
     * <p>&lt;br&gt;
     * &lt;details&gt;
     * &lt;summary&gt;Description&lt;/summary&gt;
     * 
     * <p>This API is supposed to be called from within the implementation of the jwk set endpoint of the
     * service where the service that supports OpenID Connect must expose its JWK Set information so that
     * client applications can verify signatures by the service and encrypt their requests to the service.
     * The URI of the endpoint can be found as the value of `jwks_uri` in [OpenID Provider Metadata](https://openid.net/specs/openid-connect-discovery-1_0.html#ProviderMetadata)
     * if the service supports [OpenID Connect Discovery 1.0](https://openid.net/specs/openid-connect-discovery-1_0.html).
     * 
     * <p>&lt;/details&gt;
     * 
     * @param includePrivateKeys The boolean value that indicates whether the response should include the private keys associated with the service or not. If `true`, the private keys are included in the response. The default value is `false`.
     * @param pretty This boolean value indicates whether the JSON in the response should be formatted or not. If `true`, the JSON in the response is pretty-formatted. The default value is `false`.
     * @param serviceId A service ID.
     * @param serverURL Overrides the server URL.
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public ServiceJwksGetApiResponse get(
            @Nullable Boolean includePrivateKeys, @Nullable Boolean pretty,
            @Nonnull String serviceId, @Nullable String serverURL) throws Exception {
        ServiceJwksGetApiRequest request = new ServiceJwksGetApiRequest(includePrivateKeys, pretty, serviceId);
        RequestOperation<ServiceJwksGetApiRequest, ServiceJwksGetApiResponse> operation
              = new ServiceJwksGetApiOperation(sdkConfiguration, serverURL);
        return operation.handleResponse(operation.doRequest(request));
    }

}
